name: Build and publish gttp

# Runs the offline build (fixtures + already-finalized cached pages) and
# publishes site/ to GitHub Pages. Manual only — trigger from the Actions tab.
# Nothing in this repo builds on a schedule; see README's "Summaries are
# permanent" section for why.
on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never run two Pages deploys at once.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: pip install -e .

      - name: Build site
        # CI builds offline: live Reddit needs a real (headed) browser, which a
        # headless datacenter runner can't provide, and Reddit blocks such IPs
        # anyway. This publishes from bundled fixtures + cached pages; live
        # refreshes happen on the self-hosted container (see README).
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: gttp build --offline

      - uses: actions/configure-pages@v5

      - uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - id: deploy
        uses: actions/deploy-pages@v4
